//! The gwsim desktop application (DESIGN section 16).
//!
//! A placeholder until phase P6. `eframe` is deliberately not a
//! dependency yet, so that builds stay fast until the app is actually
//! built.

fn main() {
    println!("gwsim desktop: not yet implemented (phase P6)");
}
