# Rust build output
/target/
**/*.rs.bk
*.pdb

# insta snapshots awaiting review
*.pending-snap
*.snap.new

# Design research: stays local (DESIGN Q42)
/research/

# Extractor cache: raw wiki HTML, never committed (DESIGN §8.10)
/.cache/

# OS files
Thumbs.db
Desktop.ini

# gwsim check writes its report here by default.
gwsim-check.json
